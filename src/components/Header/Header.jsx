import React from 'react';
import { HeaderWrapper } from './Header.styled';

const Header = () => {
  return (
    <HeaderWrapper>
      <h1>My React App</h1>
    </HeaderWrapper>
  );
};

export default Header;
